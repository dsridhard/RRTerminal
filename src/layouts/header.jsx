import React from "react";
import { Grid, AppBar, Toolbar, Typography } from "@mui/material";
const Header = () => {
  return (
    <nav className="navbar">
      <h2
        className="col text-center text-danger user-select-none "
        title="Retiring Room "
      >
        INDIAN RAILWAY'S RETIRING ROOM
      </h2>
    </nav>
  );
};

export default Header;
